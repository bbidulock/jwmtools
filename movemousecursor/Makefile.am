
AM_CFLAGS = $(JWMT_CFLAGS)
AM_CPPFLAGS = -DVERSION=\"${VERSION}\" -DPREFIX=\"${PREFIX}\"
AM_LDFLAGS = $(JWMT_LIBS)

bin_PROGRAMS = movemousecursor

AM_INSTALLCHECK_STD_OPTIONS_EXEMPT = movemousecursor

movemousecursor_SOURCES = movemousecursor.c
